# ============================================================================ #
#  AI-Antivirus — зависимости
#
#  Установка:   pip install -r requirements.txt
#  Python:      3.10+  (проверено на 3.11 / 3.12 / 3.13)
#
#  Библиотеки разбиты на блоки: обязательные, опциональные и для разработки.
#  Если опциональный пакет не установлен, соответствующая функция отключается
#  gracefully (с предупреждением в лог), а не падает.
# ============================================================================ #

# ---------------------------- ОБЯЗАТЕЛЬНЫЕ -------------------------------- #

# Разбор формата Portable Executable: заголовки, секции, импорты, ресурсы, TLS.
pefile==2024.8.26

# Классификатор (RandomForestClassifier, Pipeline, метрики, кросс-валидация).
scikit-learn>=1.4,<1.7

# Численные вычисления: энтропия, матрицы признаков, статистика.
numpy>=1.26,<3.0

# Работа с датасетами (CSV/EMBER), сводные таблицы, отчёты.
pandas>=2.1,<3.0

# Сериализация обученной модели (joblib.dump / joblib.load).
# ВАЖНО: в исходном ТЗ была опечатка "jober" — правильного пакета не существует.
joblib>=1.3,<2.0

# ---------------------------- ОПЦИОНАЛЬНЫЕ -------------------------------- #
# Раскомментируйте то, что нужно. Без них проект работает в базовом режиме.

# Второй ML-бэкенд (python main.py train --backend lightgbm).
lightgbm>=4.0,<5.0

# Шифрование объектов карантина AES-256-GCM.
# Без него карантин использует обратимую XOR-обфускацию (НЕ криптостойкую).
cryptography>=42.0

# Сигнатурные правила поверх ML-слоя (в прототипе не используются,
# но это логичный следующий шаг развития).
# yara-python>=4.5

# ------------------------- ТОЛЬКО ДЛЯ РАЗРАБОТКИ -------------------------- #
# Для продакшн-сборки устанавливайте отдельной командой:
#   pip install -r requirements.txt -r requirements-dev.txt

# pytest>=8.0            # модульные и интеграционные тесты
# ruff>=0.5              # линтер + форматтер
# mypy>=1.10             # статическая проверка типов
